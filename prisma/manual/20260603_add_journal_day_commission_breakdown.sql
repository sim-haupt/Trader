ALTER TABLE "JournalDay"
ADD COLUMN IF NOT EXISTS "commissionFee" DECIMAL(18, 4) NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "ecnFee" DECIMAL(18, 4) NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "catFee" DECIMAL(18, 4) NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "tafFee" DECIMAL(18, 4) NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "nsccFee" DECIMAL(18, 4) NOT NULL DEFAULT 0;

UPDATE "JournalDay"
SET "tafFee" = "finraFee"
WHERE "tafFee" = 0 AND "finraFee" <> 0;
